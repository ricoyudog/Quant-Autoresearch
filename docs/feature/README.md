> Status: historical

# Legacy Feature Workspaces

The `docs/feature/` tree is retained for V2 traceability and historical planning context.

- These workspaces are no longer the default canonical repository entrypoint.
- Start current repository navigation at `docs/index.md`.
- Start current execution-spec navigation at `specs/index.md`.
- Files in this tree are preserved as historical planning records and should be read through their status banners first.
