# Architecture Redirect

> Status: legacy entry
> Canonical path: `docs/architecture/index.md`

This root path is retained temporarily for compatibility while `docs/architecture/index.md` becomes the canonical architecture entrypoint.

- For the runtime / agent operating contract, use `program.md`.
- For the repository docs map, use `docs/index.md`.
